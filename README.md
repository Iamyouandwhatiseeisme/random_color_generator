# random_color_generator

- Generates random colors when the screen is clicked

# Usage

1. Click screen anywhere to generate random color
2. Click Realign gradients to change the direction of gradients to random direction
3. Click language button to change the current language
